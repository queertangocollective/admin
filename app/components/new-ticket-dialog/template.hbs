{{#form-for class='new-ticket-dialog' name='ticket' onsubmit=(action 'submit') as |f|}}
  <header>{{t 'ticket.create.title' icon=(icon 'ticket') htmlSafe=true}}</header>
  <div class="body grid-column">
    {{f.text 'description'}}
    <div class='grid-row'>
      {{f.currency 'cost' 'currency' currencies=(list 'USD' 'EUR')}}
      {{f.number 'quantity'}}
    </div>
    <div class='grid-row'>
      {{f.datetime 'validFrom' timezone=timezone}}
      {{f.datetime 'validTo' timezone=timezone}}
    </div>
  </div>
  <footer>
    {{#f.submit}}{{t 'ticket.create.submit'}}{{/f.submit}}
    <button type='reset' onclick={{dismiss}}>{{t 'ticket.create.cancel'}}</button>
  </footer>
{{/form-for}}
