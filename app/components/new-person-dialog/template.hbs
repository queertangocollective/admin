{{#form-for onsubmit=(action 'submit') as |f|}}
  <header>{{icon 'person'}} Add Person</header>
  <div class="body">
    {{f.text 'name'}}
  </div>
  <footer>
    {{f.submit 'Add Person'}}
    <button type='reset' onclick={{dismiss}}>Cancel</button>
  </footer>
{{/form-for}}
